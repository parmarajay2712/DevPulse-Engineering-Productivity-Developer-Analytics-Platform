import { describe, it, expect } from 'vitest';

// Dummy test to prove Vitest is set up
describe('Dashboard component', () => {
  it('should pass a basic sanity check', () => {
    expect(true).toBe(true);
  });
});
